{
  "name": "nexo-support",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cd bot && bun run dev",
    "start": "cd bot && bun run start",
    "lint": "cd bot && bun run lint",
    "db:push": "cd bot && bun run db:push",
    "db:migrate": "cd bot && bun run db:migrate"
  }
}
